type AnalystWorkJob {
    total_work_job: Int!
    total_work_job_hiring: Int!
    total_cv_applied: Int!
    total_cv_new_applied: Int!
}

type LogHistoryCoin {
    user_id: ID
    coin_used: Int
    message: String
    created_at: String
}

type LogHistoryCoinPaginator {
    paginatorInfo: PaginatorInfo!
    data: [LogHistoryCoin!]!
}
